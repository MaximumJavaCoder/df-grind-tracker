const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)], uid=()=>crypto.randomUUID?crypto.randomUUID():String(Date.now()+Math.random());
const key='dfDialV8';let view='home', beanTab='current', maintTab='grinder', plus=false, timer={running:false,start:0,elapsed:0,int:null,beanId:null};
const models=['DF54','DF64','DF64V','DF83','DF83V']; const today=()=>new Date().toISOString().slice(0,10); const iso=()=>new Date().toISOString();
function seed(){let g=uid(), b1=uid(), b2=uid(), b3=uid(), t=iso();return {currentBeanId:b1,profile:{brew:['Espresso'],machine:{manufacturer:'',model:''},grinders:[{id:g,model:'DF64',name:'DF64 Gen 2',burrType:'Stainless Steel',burrOther:'',profile:'Multipurpose (espresso)'}],flavour:{roast:50,notes:['Chocolate','Sweet']},workflow:['WDT','RDT']},beans:[{id:b1,status:'current',name:'Ethiopia Chelbesa',roaster:'Konga',origin:'Ethiopia',process:'Washed',roastLevel:'Light',roastDate:'2026-05-24',grinderId:g},{id:b2,status:'current',name:'Colombia Los Naranjos',roaster:'Onyx Coffee Lab',origin:'Colombia',process:'Washed',roastLevel:'Medium',roastDate:'2026-05-20',grinderId:g},{id:b3,status:'current',name:'Brazil Fazenda',roaster:'DF Coffee Co.',origin:'Brazil',process:'Natural',roastLevel:'Dark',roastDate:'2026-05-18',grinderId:g}],shots:[{id:uid(),beanId:b1,grinderId:g,grind:18.6,dose:18,yieldOut:36.2,time:29.0,rating:8.2,createdAt:t},{id:uid(),beanId:b2,grinderId:g,grind:17.8,dose:18,yieldOut:36,time:32,rating:7.4,createdAt:t}],maintenance:[{id:uid(),group:'grinder',name:'Grinder deep clean',item:'DF64 Gen 2',intervalDays:45,lastDone:today()},{id:uid(),group:'grinder',name:'Chute cleaning',item:'DF64 Gen 2',intervalDays:7,lastDone:today()},{id:uid(),group:'espresso',name:'Backflushing',item:'Espresso machine',intervalDays:7,lastDone:today()}],settings:{rateReminder:true,rateDelay:7}}}
let state=load();function load(){try{let s=localStorage.getItem(key)||localStorage.getItem('dfDialV7'); if(s){let o=JSON.parse(s); if(!o.profile){let fresh=seed(); o.profile=fresh.profile; o.settings=fresh.settings; o.beans=(o.beans||fresh.beans).map(b=>({...b,status:b.status==='archived'?'archived':'current'})); o.maintenance=fresh.maintenance;} return o}}catch(e){} return seed()}function save(){localStorage.setItem(key,JSON.stringify(state))}function esc(s=''){return String(s).replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}function h(ms=12){try{navigator.vibrate&&navigator.vibrate(ms)}catch(e){}}function toast(m){let t=$('#toast');t.textContent=m;t.style.display='block';setTimeout(()=>t.style.display='none',1800)}
const bean=id=>state.beans.find(b=>b.id===id), grinder=id=>state.profile.grinders.find(g=>g.id===id)||state.profile.grinders[0]||{}, shotsFor=id=>state.shots.filter(s=>s.beanId===id).sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt)), lastShot=id=>shotsFor(id)[0], currentBean=()=>bean(state.currentBeanId)||state.beans.find(b=>b.status!=='archived')||state.beans[0];
function ratio(s){let d=+s.dose,y=+s.yieldOut;return d&&y?'1:'+(y/d).toFixed(1):'—'}function fmt(sec){sec=+(sec||0); if(sec<60)return sec.toFixed(1).padStart(4,'0'); let m=Math.floor(sec/60),r=(sec%60).toFixed(1).padStart(4,'0'); return `${m}:${r}`}function dueInfo(){let x=null;state.maintenance.forEach(m=>{let d=new Date(m.lastDone);d.setDate(d.getDate()+ +m.intervalDays); if(!x||d<x.date)x={m,date:d}});let days=x?Math.ceil((x.date-new Date())/86400000):0;return {x,days}}
function header(){return `<div class="hero"><img class="wordmark" src="dfgrinderslogo.png"></div>`}function icon(n){return {home:'<svg viewBox="0 0 24 24"><path d="M3 10.8 12 3l9 7.8v9.8a1 1 0 0 1-1 1h-5.2v-6.2H9.2v6.2H4a1 1 0 0 1-1-1z"/></svg>',beans:'<svg viewBox="0 0 24 24"><path d="M15.8 3.2c3.1 1.2 4.6 5.7 3.2 10s-5 7-8.2 5.8-4.7-5.7-3.3-10 5.1-7 8.3-5.8Z"/><path d="M8.1 18.5c2.3-2.5 4-5.2 5.1-8.4.7-2 1-4 1.1-6"/></svg>',maintenance:'<svg viewBox="0 0 24 24"><path d="M14.8 6.1a4.5 4.5 0 0 0-5.4 5.4l-5.9 5.9 3.1 3.1 5.9-5.9a4.5 4.5 0 0 0 5.4-5.4l-3 3-2.1-2.1z"/></svg>',more:'<svg viewBox="0 0 24 24"><path d="M4 7h16M4 12h16M4 17h16"/></svg>'}[n]||'<svg viewBox="0 0 24 24"><path d="M6 8h12v5.5a6 6 0 0 1-12 0z"/><path d="M18 10h2.2a2.3 2.3 0 0 1 0 4.6H18M8 20h8M10 4h4"/></svg>'}function nav(){return `<nav class="nav"><button class="${view==='home'?'active':''}" onclick="go('home')">${icon('home')}</button><button class="${view==='beans'?'active':''}" onclick="go('beans')">${icon('beans')}</button><button class="plus" onclick="togglePlus()">+</button><button class="${view==='maintenance'?'active':''}" onclick="go('maintenance')">${icon('maintenance')}</button><button class="${view==='more'?'active':''}" onclick="go('more')">${icon('more')}</button></nav>${plus?`<div class="plus-menu"><button onclick="plus=false;go('shot')">Log Shot</button><button onclick="plus=false;openBean()">New Bean</button></div>`:''}`}
function go(v){view=v;plus=false;render()}function togglePlus(){h(15);plus=!plus;render()}function render(){save();$('#app').innerHTML=`<div class="wrap">${header()}${views[view]?views[view]():views.home()}${nav()}</div>`;bind()}
const views={home(){let b=currentBean(),s=b&&lastShot(b.id),g=s?grinder(s.grinderId):grinder(b?.grinderId),month=new Date().toISOString().slice(0,7),d=dueInfo();return `<section class="card click" onclick="summary('${b?.id||''}')"><h2>Current Bean</h2><div class="bean-card" style="padding:0;grid-template-columns:66px 1fr auto"><div class="bag">DF</div><div><div class="bean-title">${esc(b?.name||'No current bean')}</div><p>${esc(b?.roaster||'')}<br>${esc([b?.origin,b?.process].filter(Boolean).join(' • '))}</p></div><span class="pill">${esc(b?.roastLevel||'')}</span></div></section><section class="card click" onclick="summary('${b?.id||''}')"><h2>Last Used Settings</h2><div class="row" style="justify-content:space-between"><div><b>${esc(g?.name||g?.model||'—')}</b><p>${esc([g?.burrType,g?.profile].filter(Boolean).join(' • '))}</p></div><div style="font-size:54px;font-weight:300">${s?s.grind:'—'}</div></div><div class="grid2"><p>Dose<br><b>${s?s.dose:'—'}g</b></p><p>Yield<br><b>${s?s.yieldOut:'—'}g</b></p><p>Ratio<br><b>${s?ratio(s):'—'}</b></p><p>Time<br><b>${s?fmt(s.time):'—'}s</b></p></div></section><div class="grid3"><button class="stat" onclick="go('beans')"><span>Current Beans</span><b>${state.beans.filter(b=>b.status!=='archived').length}</b></button><button class="stat" onclick="go('history')"><span>Shots This Month</span><b>${state.shots.filter(x=>x.createdAt.slice(0,7)===month).length}</b></button><button class="stat" onclick="go('maintenance')"><span>Maintenance Due</span><b>${d.days<=0?'Now':d.days+'d'}</b></button></div>${profileCard()}<section class="card"><h2>Shot History</h2>${state.shots.slice().sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt)).slice(0,5).map(shotRow).join('')||'<p>No shots yet.</p>'}</section>`},beans(){return beanPage()},history(){return `<section class="card"><h2>Last 20 Shots</h2>${state.shots.slice().sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt)).slice(0,20).map(shotRow).join('')||'<p>No shots yet.</p>'}</section>`},shot(){return shotForm()},maintenance(){return maintView()},more(){return `<section class="card"><h2>Settings</h2><label><input type="checkbox" ${state.settings.rateReminder?'checked':''} onchange="state.settings.rateReminder=this.checked;save()" style="width:auto"> Ask me to rate my shot later</label><label>Rating reminder delay</label><select onchange="state.settings.rateDelay=this.value;save()"><option value="5" ${state.settings.rateDelay==5?'selected':''}>5 minutes</option><option value="10" ${state.settings.rateDelay==10?'selected':''}>10 minutes</option></select><button class="btn full secondary" onclick="exportData()">Export Backup</button><button class="btn full secondary" onclick="localStorage.removeItem(key);location.reload()">Reset Demo Data</button></section>`}};
function profileCard(){let p=state.profile;return `<section class="card"><h2>Profile</h2><p>${p.brew.join(' • ')||'No brew methods selected'}</p><p>${esc(p.machine.manufacturer)} ${esc(p.machine.model)}</p><p>${p.grinders.map(g=>esc(g.name||g.model)).join(' • ')}</p><button class="btn secondary" onclick="editProfile()">Edit Profile</button></section>`}
function beanPage(){let arch=beanTab==='archive', list=state.beans.filter(b=>arch?b.status==='archived':b.status!=='archived');return `<section class="card"><h2>Beans</h2><div class="tabs"><button class="${!arch?'sel':''}" onclick="beanTab='current';render()">Current <span>${state.beans.filter(b=>b.status!=='archived').length}</span></button><button class="${arch?'sel':''}" onclick="beanTab='archive';render()">Archive <span>${state.beans.filter(b=>b.status==='archived').length}</span></button></div><input placeholder="Search ${arch?'archive':'current'} beans" oninput="filterBeans(this.value)"><div style="margin:12px 0"><button class="btn secondary" onclick="openBean()">Add Bean</button></div><div id="beanList">${list.map(beanCard).join('')||'<p>No beans here.</p>'}</div></section>`}
function beanCard(b){let s=lastShot(b.id);return `<div class="card bean-card click" onclick="openBean('${b.id}')"><div class="bag">DF</div><div><div class="bean-title">${esc(b.name)}</div><p>${esc(b.roaster)}<br>${esc(b.origin)} · ${s?esc(s.grind)+' setting':'No shots yet'}</p></div><span class="pill">${esc(b.roastLevel)}</span></div>`}
function shotRow(s){let b=bean(s.beanId);return `<div class="card click" onclick="openShot('${s.id}')"><div class="row" style="justify-content:space-between"><b>${esc(b?.name||'Bean')}</b><b>${s.grind}</b></div><p>${esc(grinder(s.grinderId).name||grinder(s.grinderId).model)} · ${s.dose}g → ${s.yieldOut}g · ${ratio(s)} · ${fmt(s.time)}s · Rating ${s.rating||'—'}</p><small>${new Date(s.createdAt).toLocaleString()}</small></div>`}
function shotForm(){let b=currentBean(), s=b&&lastShot(b.id), g=s?grinder(s.grinderId):grinder(b?.grinderId), grind=s?.grind||14.7;return `<section class="card"><h2>Log Shot</h2><div class="seg"><button class="sel">Existing Bean</button><button onclick="openBean()">New Bean</button></div><label>Bean</label><select id="shotBean" onchange="selectShotBean(this.value)">${state.beans.filter(x=>x.status!=='archived').map(x=>`<option value="${x.id}" ${x.id===b?.id?'selected':''}>${esc(x.name)}</option>`).join('')}</select><div id="shotFields">${shotFields(b?.id,grind,s?.dose||18,s?.yieldOut||36,g?.id)}</div></section>`}
function shotFields(beanId,grind,dose,yieldOut,grinderId){return `<div class="card" style="margin-left:0;margin-right:0"><div class="setting-card"><div><label>Grinder</label><select id="shotGrinder">${state.profile.grinders.map(g=>`<option value="${g.id}" ${g.id===grinderId?'selected':''}>${esc(g.name||g.model)}</option>`).join('')}</select></div><div class="setting-num"><label>Setting</label><input id="grindInput" type="number" step="0.1" value="${grind}" oninput="setRuler(+this.value)"></div><div class="ruler" id="ruler"><div class="track" id="rulerTrack"></div><div class="center-line"></div></div></div><div class="grid2"><div><label>Dose</label><input id="dose" type="number" step="0.1" value="${dose}"></div><div><label>Yield</label><input id="yieldOut" type="number" step="0.1" value="${yieldOut}"></div></div><div id="timerBox">${timerMarkup()}</div><label>Taste / Result Rating <span id="ratingVal">7.5</span></label><div class="rating-line"></div><input class="slider" id="rating" type="range" min="1" max="10" step="0.1" value="7.5" oninput="$('#ratingVal').textContent=this.value"><button class="btn full" onclick="saveShot()">Save Shot</button></div>`}
function timerMarkup(){return `<div class="timer ${timer.running?'running':''}" onclick="toggleTimer()"><div class="timer-inner"><div><div class="timer-time" id="timeTxt">${fmt(timer.elapsed/1000)}</div><div class="timer-label">TAP TO ${timer.running?'STOP':'START'}</div></div></div></div><button class="timer-button ${timer.running?'stop':''}" onclick="event.stopPropagation();toggleTimer()"></button>`}
function toggleTimer(){h(25); if(timer.running){timer.elapsed+=Date.now()-timer.start;timer.running=false;clearInterval(timer.int);$('#timeTxt')&&($('#timeTxt').textContent=fmt(timer.elapsed/1000));$('#timerBox').innerHTML=timerMarkup();return} timer.running=true;timer.start=Date.now();timer.int=setInterval(()=>{$('#timeTxt')&&($('#timeTxt').textContent=fmt((timer.elapsed+Date.now()-timer.start)/1000))},100);$('#timerBox').innerHTML=timerMarkup()}
function selectShotBean(id){state.currentBeanId=id;let s=lastShot(id), b=bean(id), g=s?grinder(s.grinderId):grinder(b.grinderId);$('#shotFields').innerHTML=shotFields(id,s?.grind||14.7,s?.dose||18,s?.yieldOut||36,g?.id);bindRuler(s?.grind||14.7)}
function saveShot(){let beanId=$('#shotBean').value, val=+$('#grindInput').value, time=timer.running?(timer.elapsed+Date.now()-timer.start)/1000:timer.elapsed/1000; state.shots.unshift({id:uid(),beanId,grinderId:$('#shotGrinder').value,grind:+val.toFixed(1),dose:+$('#dose').value,yieldOut:+$('#yieldOut').value,time:+time.toFixed(1),rating:+$('#rating').value,createdAt:iso()});state.currentBeanId=beanId;timer={running:false,start:0,elapsed:0,int:null};save();toast('Shot saved'); if(state.settings.rateReminder) setTimeout(()=>toast('Rate your last shot when ready.'), Math.min((+state.settings.rateDelay||7)*60000,600000));go('home')}
function bindRuler(val=+($('#grindInput')?.value||15)){let el=$('#ruler'), tr=$('#rulerTrack'); if(!el||!tr)return; let min=0,max=100,step=30; tr.innerHTML=''; for(let i=min;i<=max;i++){let m=document.createElement('div');m.className='mark '+(i%5===0?'major':''); if(i%5===0)m.innerHTML=`<span>${i}</span>`; tr.appendChild(m)} let current=+val,lastWhole=Math.round(current); const draw=()=>{let w=step, offset=el.clientWidth/2-current*w; tr.style.transform=`translateX(${offset}px)`; $('#grindInput').value=current.toFixed(1)}; draw(); let startX,startVal; el.onpointerdown=e=>{el.setPointerCapture(e.pointerId);startX=e.clientX;startVal=current}; el.onpointermove=e=>{if(startX==null)return; current=Math.max(min,Math.min(max,startVal-(e.clientX-startX)/step)); current=Math.round(current*10)/10; if(Math.floor(current)!==lastWhole){lastWhole=Math.floor(current);h(5)} draw()}; el.onpointerup=e=>{startX=null; h(8)}; el.onpointercancel=()=>startX=null; window.setRuler=v=>{current=Math.max(min,Math.min(max,Math.round(v*10)/10));draw();h(8)}}
function maintView(){let list=state.maintenance.filter(m=>m.group===maintTab);return `<section class="card"><h2>Maintenance</h2><div class="tabs"><button class="${maintTab==='grinder'?'sel':''}" onclick="maintTab='grinder';render()">Grinder</button><button class="${maintTab==='espresso'?'sel':''}" onclick="maintTab='espresso';render()">Espresso Machine</button></div>${list.map(m=>maintRow(m)).join('')}<button class="btn secondary full" onclick="editMaint()">Add Custom Maintenance</button></section><section class="card guide"><h2>Guides & Tips</h2><p>DF54: chute cleaning, deep cleaning video, declumper installation.</p><p>DF64: product manual, grind size guide, dial indicator installation.</p><p><a href="https://dfgrinders.ca/pages/manuals-and-guides" target="_blank">Open DF Grinders Manuals & Guides</a></p></section>`}
function maintRow(m){let d=new Date(m.lastDone);d.setDate(d.getDate()+ +m.intervalDays);let days=Math.ceil((d-new Date())/86400000);return `<div class="card click maint-row" onclick="editMaint('${m.id}')"><div class="ico">${m.group==='grinder'?'⚙':'☕'}</div><div><b>${esc(m.name)}</b><p>${esc(m.item)}<br>${days<=0?'Due now':'Due in '+days+' days'} · ${d.toLocaleDateString()}</p></div><span>›</span></div>`}
function editMaint(id){let m=id?state.maintenance.find(x=>x.id===id):{id:'',group:maintTab,name:'',item:'',intervalDays:7,lastDone:today()};modal(`<h2>${id?'Edit':'Add'} Maintenance</h2><label>Type</label><select id="mg"><option value="grinder" ${m.group==='grinder'?'selected':''}>Grinder</option><option value="espresso" ${m.group==='espresso'?'selected':''}>Espresso Machine</option></select><label>Name</label><input id="mn" value="${esc(m.name)}"><label>Item</label><input id="mi" value="${esc(m.item)}"><label>Frequency (days)</label><input id="mf" type="number" value="${esc(m.intervalDays)}"><label>Last done</label><input id="ml" type="date" value="${esc(m.lastDone)}"><button class="btn full" onclick="saveMaint('${id||''}')">Save</button>${id?`<button class="btn secondary full" onclick="doneMaint('${id}')">Mark Done Today</button>`:''}`)}
function saveMaint(id){let m={id:id||uid(),group:$('#mg').value,name:$('#mn').value,item:$('#mi').value,intervalDays:+$('#mf').value,lastDone:$('#ml').value}; if(id){Object.assign(state.maintenance.find(x=>x.id===id),m)}else state.maintenance.push(m);closeModal();render()}function doneMaint(id){let m=state.maintenance.find(x=>x.id===id);m.lastDone=today();closeModal();render()}
function openBean(id){let b=id?bean(id):{id:'',status:'current',name:'',roaster:'',origin:'',process:'',roastLevel:'Medium',roastDate:today(),grinderId:state.profile.grinders[0]?.id};modal(`<h2>${id?'Edit Bean':'New Bean'}</h2><label>Name</label><input id="bn" value="${esc(b.name)}"><label>Roaster</label><input id="br" value="${esc(b.roaster)}"><label>Origin</label><input id="bo" value="${esc(b.origin)}"><label>Process</label><input id="bp" value="${esc(b.process)}"><label>Roast Level</label><select id="bl"><option ${b.roastLevel==='Light'?'selected':''}>Light</option><option ${b.roastLevel==='Medium'?'selected':''}>Medium</option><option ${b.roastLevel==='Dark'?'selected':''}>Dark</option></select><label>Roast Date</label><input id="bd" type="date" value="${esc(b.roastDate)}"><label>Status</label><select id="bs"><option value="current" ${b.status!=='archived'?'selected':''}>Current</option><option value="archived" ${b.status==='archived'?'selected':''}>Archived</option></select><button class="btn full" onclick="saveBean('${id||''}')">Save Bean</button>`)}function saveBean(id){let b={id:id||uid(),status:$('#bs').value,name:$('#bn').value,roaster:$('#br').value,origin:$('#bo').value,process:$('#bp').value,roastLevel:$('#bl').value,roastDate:$('#bd').value,grinderId:state.profile.grinders[0]?.id}; if(id)Object.assign(bean(id),b); else state.beans.push(b); if(b.status==='current')state.currentBeanId=b.id; closeModal();render()}
function openShot(id){let s=state.shots.find(x=>x.id===id); if(!s)return; modal(`<h2>Edit Shot</h2><label>Grind</label><input id="esg" type="number" step="0.1" value="${s.grind}"><label>Dose</label><input id="esd" type="number" step="0.1" value="${s.dose}"><label>Yield</label><input id="esy" type="number" step="0.1" value="${s.yieldOut}"><label>Time</label><input id="est" type="number" step="0.1" value="${s.time}"><label>Rating</label><input id="esr" type="number" step="0.1" min="1" max="10" value="${s.rating||7.5}"><button class="btn full" onclick="saveEditShot('${id}')">Save Shot</button>`)}function saveEditShot(id){let s=state.shots.find(x=>x.id===id);Object.assign(s,{grind:+$('#esg').value,dose:+$('#esd').value,yieldOut:+$('#esy').value,time:+$('#est').value,rating:+$('#esr').value});closeModal();render()}
function summary(id){let b=bean(id); if(!b)return; let s=lastShot(id),g=s?grinder(s.grinderId):grinder(b.grinderId),shots=shotsFor(id),avg=shots.length?(shots.reduce((a,x)=>a+(+x.time||0),0)/shots.length).toFixed(1):'—',d=dueInfo();modal(`<h2>Current Bean Profile</h2><div class="bean-card" style="padding:0"><div class="bag">DF</div><div><div class="bean-title">${esc(b.name)}</div><p>${esc(b.roaster)}<br>${esc(b.origin)} • ${esc(b.process)}</p></div><span class="pill">${esc(b.roastLevel)}</span></div><h3>Equipment</h3><div class="detail"><div><span>Grinder</span>${esc(g.name||g.model)}</div><div><span>Burrs</span>${esc([g.burrType,g.profile].filter(Boolean).join(' • '))}</div><div><span>Machine</span>${esc(state.profile.machine.manufacturer+' '+state.profile.machine.model)}</div></div><h3>Last Used Settings</h3><div class="detail"><div><span>Setting</span>${s?s.grind:'—'}</div><div><span>Dose/Yield</span>${s?`${s.dose}g → ${s.yieldOut}g`: '—'}</div><div><span>Ratio</span>${s?ratio(s):'—'}</div><div><span>Time</span>${s?fmt(s.time)+'s':'—'}</div></div><h3>Statistics & Maintenance</h3><div class="detail"><div><span>Total shots</span>${shots.length}</div><div><span>Average time</span>${avg}s</div><div><span>Next maintenance</span>${d.days<=0?'Due now':d.days+' days'}</div></div><button class="btn full secondary" onclick="openBean('${id}')">Edit Bean</button>`)}
function editProfile(){let p=state.profile; const chk=(arr,v)=>arr.includes(v)?'checked':'';modal(`<h2>Edit Profile</h2><label>Brew Methods</label><div class="profile-chips">${['Pour Over','French Press','Espresso','Aeropress','Moka Pot','Other'].map(x=>`<label><input type="checkbox" name="brew" value="${x}" ${chk(p.brew,x)}>${x}</label>`).join('')}</div><h3>Espresso Machine</h3><label>Manufacturer</label><input id="pmake" value="${esc(p.machine.manufacturer)}"><label>Model</label><input id="pmodel" value="${esc(p.machine.model)}"><h3>Grinder</h3><label>Model</label><select id="gmodel">${models.map(m=>`<option ${p.grinders[0]?.model===m?'selected':''}>${m}</option>`).join('')}</select><label>Burr Type</label><select id="gburr"><option ${p.grinders[0]?.burrType==='Red Titanium'?'selected':''}>Red Titanium</option><option ${p.grinders[0]?.burrType==='Stainless Steel'?'selected':''}>Stainless Steel</option><option ${p.grinders[0]?.burrType==='Other'?'selected':''}>Other</option></select><label>Other Burr</label><input id="gother" value="${esc(p.grinders[0]?.burrOther||'')}"><label>Burr Profile</label><select id="gprof"><option ${p.grinders[0]?.profile==='Brew'?'selected':''}>Brew</option><option ${p.grinders[0]?.profile==='Multipurpose (espresso)'?'selected':''}>Multipurpose (espresso)</option></select><label>Coffee Flavour Preference: Dark ← → Light</label><input id="roastpref" class="slider" type="range" min="0" max="100" value="${p.flavour.roast}"><label>Flavours</label><div class="profile-chips">${['Chocolate','Fruity','Floral','Sweet','Funky'].map(x=>`<label><input type="checkbox" name="flav" value="${x}" ${chk(p.flavour.notes,x)}>${x}</label>`).join('')}</div><label>Workflow</label><div class="profile-chips">${['Blind shaking','WDT','RDT','Slow feed','Distributor tool','Pre-infusion enabled'].map(x=>`<label><input type="checkbox" name="flow" value="${x}" ${chk(p.workflow,x)}>${x}</label>`).join('')}</div><button class="btn full" onclick="saveProfile()">Save Profile</button>`)}
function saveProfile(){let g=state.profile.grinders[0]||{id:uid()};Object.assign(g,{model:$('#gmodel').value,name:$('#gmodel').value,burrType:$('#gburr').value,burrOther:$('#gother').value,profile:$('#gprof').value});state.profile={brew:$$('input[name=brew]:checked').map(x=>x.value),machine:{manufacturer:$('#pmake').value,model:$('#pmodel').value},grinders:[g],flavour:{roast:+$('#roastpref').value,notes:$$('input[name=flav]:checked').map(x=>x.value)},workflow:$$('input[name=flow]:checked').map(x=>x.value)};closeModal();render()}
function modal(html){document.body.insertAdjacentHTML('beforeend',`<div class="modal-bg" id="modal"><div class="modal"><div class="modal-head"><span></span><button onclick="closeModal()">×</button></div>${html}</div></div>`)}function closeModal(){$('#modal')?.remove();render()}function filterBeans(q){q=q.toLowerCase();let arch=beanTab==='archive';$('#beanList').innerHTML=state.beans.filter(b=>(arch?b.status==='archived':b.status!=='archived')&&[b.name,b.roaster,b.origin,b.process].join(' ').toLowerCase().includes(q)).map(beanCard).join('')||'<p>No matches.</p>'}function exportData(){let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(state,null,2)],{type:'application/json'}));a.download='df-dial-backup.json';a.click()}function bind(){bindRuler();} if('serviceWorker'in navigator)navigator.serviceWorker.register('sw.js'); render();
